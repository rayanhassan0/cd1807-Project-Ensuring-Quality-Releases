**/*.tfstate
**/*.json
**/*.exe
# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Ignore any .tfvars files that are generated automatically for each Terraform run. Most
# .tfvars files are managed as part of configuration and so should be included in
# version control.
#
# example.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json# Terraform local state & cache
**/.terraform/
**/.terraform.lock.hcl
**/*.tfstate
**/*.tfstate.*
**/crash.log
**/plan.out
**/destroy.out

# Do NOT commit real secrets
**/*.tfvars

# Local artifacts / submissions
submit/
zip-check/
*.zip
screenshots/

